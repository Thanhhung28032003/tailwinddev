<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body class="bg-slate-200 font-Karla">
    <div class="max-w-screen-lg mx-auto p-6">
      <h1 class="text-2xl font-bold mb-4"><%= title %></h1>
      <p class="text-gray-600 mb-6">Trang quản trị (demo) render bằng EJS.</p>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
        <% products.forEach(p => { %>
          <div class="bg-white rounded shadow p-4">
            <div class="font-semibold mb-1"><%= p.name %></div>
            <div class="text-sm text-gray-500 mb-3">ID: <%= p.id %></div>
            <div class="text-[#b97c50] font-bold"><%= (p.price).toLocaleString('vi-VN') %>đ</div>
          </div>
        <% }) %>
      </div>
    </div>
  </body>
</html>
